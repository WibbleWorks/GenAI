# Quantum-AI Course (standalone, spun out of core in P4)

**Positioning:** theoretical / contested. Research-track material — not a
hiring path. Prerequisites: Lessons 3 (NN Intro), 5 (Math Refresher) of the
core course. Core Lesson 17 is now the Frontier Map, which points here.

## Lessons

1. `lessons/quantum_ai_intersection.json` — Qubits → Qiskit 1.x patterns →
   quantum kernels (with caveats) → what *isn't* proven. Includes the
   5-minute QM primer. 5-question quiz, 60% pass.

## Rules (enforced)

- `CITATIONS.md` holds one row per advantage claim (Roadmap T6.4). No
  citation → claim is reworded or removed on the quarterly audit.
- No "exponential speedup" without "problem-dependent, contested".
- Validates with the same schema + scripts as core
  (`node scripts/validate-lessons.mjs` covers this folder).
